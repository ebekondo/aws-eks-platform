apiVersion: v1
kind: Service
metadata:
  name: aws-eks-backend-service
  labels:
    app: aws-eks-backend

spec:
  selector:
    app: aws-eks-backend

  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 8080

  type: LoadBalancer
